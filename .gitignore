# Lake local cache (use `lake exe cache get` / CI to fetch Mathlib)
.lake/

# LaTeX sync (local editor)
*.synctex.gz

# LaTeX build outputs under papers/ (source of truth: .tex)
papers/**/*.aux
papers/**/*.fdb_latexmk
papers/**/*.fls
papers/**/*.log
papers/**/*.out
papers/**/*.toc
